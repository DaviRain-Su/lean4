module

prelude
import Lean.Near.Bindings

/-!
# Lean NEAR SDK

A Lean library for writing NEAR smart contracts that compile to WASM via
the Zig backend (`lean -z` + `tools/zigc-near`).
-/

public import Lean.Near.Bindings
